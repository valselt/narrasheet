RewriteEngine On

# 1. Jika request adalah folder, biarkan (jangan diubah)
RewriteCond %{REQUEST_FILENAME} !-d

# 2. Cek apakah file dengan ekstensi .php ada
RewriteCond %{REQUEST_FILENAME}.php -f

# 3. Rewrite request tanpa ekstensi ke file .php yang sesuai
RewriteRule ^(.*)$ $1.php [L]